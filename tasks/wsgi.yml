---

- include: packages.deb.yml
  when: ansible_os_family == 'Debian'
  tags: [wsgi]

- include: virtualenv.yml
  tags: [wsgi, virtualenv]

- include: uwsgi.yml
  tags: [wsgi, uwsgi]

- include: nginx.yml
  tags: [wsgi, nginx]

- include: supervisor.deb.yml
  when: ansible_os_family == 'Debian'
  tags: [wsgi, supervisor]
